{
  "name": "twinstudio-merged",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "products:update": "node scripts/update-products.mjs",
    "products:check": "node scripts/update-products.mjs --check",
    "migrate:sources": "node scripts/migrate-sources.mjs"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
